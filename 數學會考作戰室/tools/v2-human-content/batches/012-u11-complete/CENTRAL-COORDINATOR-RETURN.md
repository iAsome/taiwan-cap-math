# Return to Central Coordinator

Return the untouched ZIP `math-v2-human-content-batch-012-u11-complete.zip` and the complete ChatGPT delivery report to the central coordinator conversation.

The coordinator must:

1. verify the ZIP SHA-256 and clean extraction result;
2. run cross-unit fingerprint comparison against all returned unit packages;
3. supply the exact expected Git HEAD;
4. review shared schemas and ordered integration;
5. explicitly approve Cursor execution;
6. keep production activation and legacy retirement disabled until the final 23-unit release gate.

Do not run the included Cursor instructions before explicit central approval.
