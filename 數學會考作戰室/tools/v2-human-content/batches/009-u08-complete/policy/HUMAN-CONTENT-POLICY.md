# HUMAN CONTENT POLICY — U08 Batch 009

## Authority

Every lecture, multiple-choice item and constructed-response item in this package is authored under:

`CHATGPT_HUMAN_AUTHORED_R1`

The authoring syllabus lock is authoritative only for IDs, order, prerequisites and CAP boundaries. Generic lock objectives and misconceptions are not accepted teaching content.

## Legacy isolation

- `legacyContentImported=false`
- `legacySemanticEvidenceAccepted=false`
- `oldProductionRuntimeModified=false`
- `deleteLegacyNow=false`
- `finalRetirementRequired=true`
- `productionActivationAllowed=false`

Old generated questions, lectures, reviews, compiled banks and generators are not sources of mathematical wording or inspiration.

## Human-authoring requirements

Each skill contains one zero-foundation lecture, exactly twelve individually written multiple-choice questions, at least two constructed-response questions and one independent semantic review per multiple-choice question. Number swaps, name swaps, cosmetic stories and parameterized question factories are prohibited.

## Review requirements

Answers are independently recomputed. Every option receives a truth value and concrete reason. Constructed-response rubrics are question-specific and include partial credit, follow-through rules, unit rules and answer-only handling.

## U08 visual boundary

The locked unit scope states `全文字描述；不含圖片。` No SVG is created for this batch. All figure IDs and drawing-spec references are null, all drawing-spec files are empty, and `figureCount=0`.

## Activation boundary

This package is immutable reviewed source. The emitter may serialize reviewed records to a local `staging/` directory for verification only. Staging output must not be included in the delivery ZIP and must never be activated without the global 23-unit release gate.

## Parallel authoring boundary

This package verifies internal duplicates only. Cross-unit exact and near-duplicate reconciliation remains a central-coordinator responsibility after all parallel packages are returned.
