# 交回總負責人

此 ZIP 是 U15 的自包含不可變來源包。請把 ZIP、SHA-256、兩次驗證 JSON 與完整回報帶回中央總負責人對話。

中央總負責人仍須執行：跨單元近似重複掃描、整合順序、實際 Git HEAD 填入、共享 schema 調和、最終全域 release gate，以及舊資料庫的一次性受控退役。

**在中央總負責人驗收前，不要把內部 Cursor 指令交給 Cursor 執行。**
