#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {spawnSync} from 'node:child_process';import {fileURLToPath,pathToFileURL} from 'node:url';
const args=process.argv.slice(2);const arg=n=>{const i=args.indexOf(n);return i>=0?args[i+1]:null};const repo=path.resolve(arg('--repo')||process.cwd());const evidence=path.resolve(arg('--evidence')||path.join(repo,'數學會考作戰室','tools','v2-human-content','global-repair','wave-1b','evidence'));const report=JSON.parse(fs.readFileSync(path.join(evidence,'wave-1b-apply-report.json'),'utf8'));const batchesRoot=path.join(repo,'數學會考作戰室','tools','v2-human-content','batches');const hook=pathToFileURL(path.join(path.dirname(fileURLToPath(import.meta.url)),'windows-posix-relative-hook.mjs')).href;
function run(root,rel,extra=[]){const r=spawnSync(process.execPath,['--import',hook,path.join(root,...rel.split('/')),...extra],{cwd:root,encoding:'utf8',env:{...process.env}});let parsed=null;try{parsed=JSON.parse(r.stdout.trim())}catch{}return{command:`node ${rel}${extra.length?' '+extra.join(' '):''}`,exitCode:r.status,signal:r.signal,parsed,stdout:r.stdout,stderr:r.stderr}}
const results=[];for(const batch of report.affectedBatches){const root=path.join(batchesRoot,batch);const first=run(root,'importer/verify-bundle.mjs');if(first.exitCode!==0){results.push({batch,first,emitter:null,second:null,pass:false});break}const emitterRel=fs.existsSync(path.join(root,'importer','emit-unit-modules.mjs'))?'importer/emit-unit-modules.mjs':'importer/emit-skill-module.mjs';const emitter=run(root,emitterRel);if(emitter.exitCode!==0){results.push({batch,first,emitter,second:null,pass:false});break}const extra=batch.startsWith('017-')?['--expect-staging']:[];const second=run(root,'importer/verify-bundle.mjs',extra);results.push({batch,first,emitter,second,pass:second.exitCode===0});if(second.exitCode!==0)break}
const status=results.length===report.affectedBatches.length&&results.every(x=>x.pass)?'PASS':'FAIL';fs.writeFileSync(path.join(evidence,'affected-batch-verifier-results.json'),JSON.stringify({status,expectedBatchCount:report.affectedBatches.length,resultCount:results.length,results},null,2)+'\n');console.log(JSON.stringify({status,expectedBatchCount:report.affectedBatches.length,completedBatchCount:results.length,failed:results.filter(x=>!x.pass).map(x=>x.batch)},null,2));if(status!=='PASS')process.exit(1);
