import fs from "node:fs";import path from "node:path";import {readJson,writeJson,sha256,listFiles,parseArgs,assert} from "./utils.mjs";
const args=parseArgs(process.argv);const root=path.resolve(args.root||"");const evidence=path.join(root,"evidence");const apply=readJson(path.join(evidence,"preview-integration-apply-report.json"));const runtime=readJson(path.join(evidence,"preview-runtime-validation.json"));const http=readJson(path.join(evidence,"preview-http-smoke.json"));assert(apply.status==="APPLIED_ISOLATED_HUMAN_PREVIEW_FILES","apply fail");assert(runtime.status==="PASS_ISOLATED_PREVIEW_RUNTIME_VALIDATION","runtime fail");assert(http.status==="PASS_HTTP_PREVIEW_SMOKE","http fail");const summary={status:"PASS_HUMAN_RUNTIME_PREVIEW_INTEGRATION_R1",contentVersion:runtime.contentVersion,counts:runtime.counts,previewRoute:"數學會考作戰室/human-preview.html",defaultStudentRouteChanged:false,defaultGeneratedV2Preserved:true,v1RollbackPreserved:true,productionActivationAllowed:false,oldDatabaseDeletionAllowed:false,mergeAllowed:false,nextAuthorizedStage:"HUMAN_PREVIEW_BROWSER_AND_UI_VALIDATION_R1",issues:[]};writeJson(path.join(evidence,"preview-integration-r1-summary.json"),summary);const manifest=listFiles(evidence).filter(f=>path.basename(f)!=="preview-integration-evidence-sha256.json").map(f=>({path:path.relative(root,f).replaceAll("\\","/"),bytes:fs.statSync(f).size,sha256:sha256(f)}));writeJson(path.join(root,"preview-integration-evidence-sha256.json"),manifest);console.log(JSON.stringify(summary,null,2));
