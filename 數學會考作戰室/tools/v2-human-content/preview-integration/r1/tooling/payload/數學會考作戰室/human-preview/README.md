# Human Runtime Preview R1

This directory is an isolated, unlinked preview client. It reads only the locked human runtime preview under `tools/v2-human-content/global-readiness/r1/runtime-preview/`.

It does not replace `index.html`, `math-bootstrap.js`, generated V2, or V1 rollback.
