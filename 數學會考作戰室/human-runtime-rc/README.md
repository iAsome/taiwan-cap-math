# Human Runtime RC R1

Release-candidate-only adapter. It reads the locked human runtime preview, never generated question banks.

- Default route is not changed.
- V1 rollback is not changed.
- Storage namespace is `capMath.humanRc.r1.*`.
- The RC route is `human-rc.html` and is noindex/unlinked.
